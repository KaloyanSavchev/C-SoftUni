using System;

public class Program
{
	public static void Main()
	{
			
		 
		int count = int.Parse(Console.ReadLine());
		int all = 0;
		int smt = 0;
		bool toe = false;
		for (int i = 1; i <= count; i++)
		{
		smt = i;
		while (i > 0)
		{
		all += i % 10;
		i = i / 10;
		}
		toe = (all == 5) || (all == 7) || (all == 11);
		Console.WriteLine("{0} -> {1}", smt, toe);
		all = 0;
		i = smt;
}
	}
}
